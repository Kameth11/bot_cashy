// Legacy entrypoint intentionally disabled.
// The only supported runtime is `src/index.js` via `npm start`.

console.error(
  'Este archivo legacy (index.js) esta deshabilitado. ' +
  'Usa `npm start` o ejecuta `node src/index.js`.'
);

process.exit(1);
